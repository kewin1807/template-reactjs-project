export type TEntry = {
  id: number;
  name: string;
  username: string;
  phone: string;
  website: string;
  company: A
  address: A;
};
export type TGetWeather = {
  // count: number;
  users: TEntry[];
};
